# Package Extraction
**Date:** July 2, 2026
**Project:** @owservable/postgres

Created as part of the owservable v3 package split (core + mongodb + postgres), extracted from the `owservable` package which remains untouched for existing consumers. Full design rationale: `owservable/improvements/2026-07-02-postgresql-backend-design.md` in the owservable repo.
